import React, {useEffect, useRef, useState} from "react";
import ReconnectingWebSocket from "reconnecting-websocket";

const DynamicMenu = ({ apiResponse }) => {



    return(
        <>

        </>
    );
}

export default DynamicMenu;